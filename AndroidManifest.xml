<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.slam5.androidapplicationlivraison"
    android:versionCode="1"
    android:versionName="1.0" >

	<uses-permission
		android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission
		android:name="android.permission.ACCESS_FINE_LOCATION" />"
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />
    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:targetPackage="com.slam5.androidapplicationlivraison" />
	
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
           <uses-library android:name="android.test.runner" />
        <activity
            android:name=".LocalisationActivity"
            android:label="@string/app_name" >
            android:name=".ListeDesMissionsActivity"
            android:label="Liste des missions" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
    
	  
          
        </activity>

        <activity
            android:name=".DetailMissionActivity"
            android:label="Détail de la mission" >
        </activity>
        <activity
            android:name=".SaisieEtatActivity"
            android:label="Saisie de l'état" >

        </activity>
       
    </application>
</manifest>
